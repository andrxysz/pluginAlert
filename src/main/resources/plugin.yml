name: Alert
version: '${project.version}'
main: br.plugins.pluginalert
commands:
  alert:
    description: Envia um alerta para um servidor especifico ou para o servidor atual.
    usage: /alert <servidor|here> <mensagem>
    permission: alert.use
permissions:
  alert.use:
    default: op
